% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/col-spec.R
\name{col_spec}
\alias{col_spec}
\title{Define one column group of a tree_table(), reactable-style}
\usage{
col_spec(name, ...)
}
\arguments{
\item{name}{Header text shown for this column group.}

\item{...}{One or more \code{\link[=col_field]{col_field()}} objects.}
}
\value{
An object of class \code{treetable_col_spec}, to pass inside a list as
\code{\link[=tree_table]{tree_table()}}'s \code{columns} argument.
}
\description{
\code{tree_table()}'s \code{columns} is a list of \code{col_spec()} groups; each group
has a header \code{name} and one or more \code{\link[=col_field]{col_field()}}s underneath it, every
one with its own source column in \code{data} (no shared naming pattern
required across groups or fields), its own aggregation, its own display
format, and optionally its own custom cell renderer.
}
\details{
A group with a single field renders as one plain column under \code{name}. A
group with several fields renders \code{name} spanning a sub-header row with
one cell per field (its \code{label}), e.g. a "Q1" group with \code{"Budget"}/
\code{"Actual"} fields.
}
\examples{
col_spec("Enero", col_field("enero_nota", aggregate = "mean"))

col_spec(
  "Q1",
  col_field("q1_budget", label = "Budget", format = column_format(type = "currency")),
  col_field("q1_actual", label = "Actual", format = column_format(type = "currency"))
)
}
