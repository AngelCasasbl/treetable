% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detail.R
\name{detail_table}
\alias{detail_table}
\title{Automatic per-leaf detail table}
\usage{
detail_table(raw_data, id_col = "id", columns = NULL)
}
\arguments{
\item{raw_data}{Data frame with the raw (pre-aggregation) rows, including
an \code{id_col} column matching the \code{id_col} used to build the
\code{\link[=tree_table]{tree_table()}} hierarchy.}

\item{id_col}{Column in \code{raw_data} identifying the rows belonging to each
leaf.}

\item{columns}{Optional character vector of columns to show (defaults to
all columns of \code{raw_data}).}
}
\value{
An object of class \code{treetable_detail_table}, to pass as the
\code{detail} argument of \code{\link[=tree_table]{tree_table()}} and \code{\link[=show_detail_modal]{show_detail_modal()}}.
}
\description{
Builds a \code{detail} spec for \code{\link[=tree_table]{tree_table()}}/\code{\link[=show_detail_modal]{show_detail_modal()}} that shows
a table of the raw rows behind a leaf, filtered by \code{id_col}. Uses
\code{reactable} when available, falling back to \code{DT}, and finally to a plain
HTML table if neither is installed.
}
\examples{
raw_data <- data.frame(id = c("a", "a", "b"), amount = c(10, 20, 5))
detail_table(raw_data, id_col = "id", columns = c("id", "amount"))
}
