% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/col-spec.R
\name{col_field}
\alias{col_field}
\title{Define one field (leaf column) inside a col_spec() group}
\usage{
col_field(
  value,
  aggregate = c("sum", "mean"),
  format = column_format(),
  label = NULL,
  cell = NULL
)
}
\arguments{
\item{value}{Name of the \code{data} column holding this field's value.}

\item{aggregate}{How this field rolls up from leaf rows to every ancestor
node: \code{"sum"} (default) or \code{"mean"}. Every node, at every depth,
aggregates directly over the leaf rows underneath it (not over its
immediate children's already-aggregated values). \code{"mean"} ignores \code{NA}
leaf values (as \code{sum} already does via \code{na.rm = TRUE}); a node with no
non-\code{NA} leaves shows as blank rather than \code{NaN}.}

\item{format}{A \code{\link[=column_format]{column_format()}} object controlling how this field is
displayed (currency, number/decimal, percentage, or text) when \code{cell}
is \code{NULL} or returns \code{NULL} for a given row.}

\item{label}{Sub-header text for this field, shown when its \code{\link[=col_spec]{col_spec()}}
group has more than one field (so they can be told apart under the
shared group header, e.g. \code{"Budget"}/\code{"Actual"}). Ignored when the
group has only this one field — the group's own \code{name} already labels
it. \code{NULL} (default) leaves the sub-header blank.}

\item{cell}{Optional \verb{function(value, row)} to fully customize how this
field renders for a given node, reactable-style: \code{value} is the
already-aggregated number for that node, and \code{row} is
\code{list(label, is_leaf)} for that node. Return:
\itemize{
\item \code{NULL} to fall back to the default, \code{format}-based rendering (the
common case: only override specific rows/values).
\item An \code{htmltools} tag/tag list (e.g. \code{htmltools::tags$span(...)}, or
\code{htmltools::HTML(...)} for a raw HTML string) for custom colors,
shapes, badges, icons, etc.
\item Anything else (a plain string or number) to override just the
displayed text, still safely escaped, bypassing \code{format}.
}}
}
\value{
An object of class \code{treetable_col_field}, to pass to \code{\link[=col_spec]{col_spec()}}.
}
\description{
Building block for \code{\link[=col_spec]{col_spec()}}: one actual value column shown under a
column group's header, with its own source column in \code{data}, aggregation,
display format, and (optionally) a custom cell renderer.
}
\details{
\code{format}'s \code{type} covers the common display kinds: \code{"number"} (plain
value; also what you want for "decimal" — just set \code{decimals}),
\code{"currency"}, \code{"percentage"}, or \code{"text"}. See \code{\link[=column_format]{column_format()}}.
}
\examples{
col_field("enero_nota", aggregate = "mean", format = column_format(decimals = 1))

col_field(
  "grade", label = "Grade",
  cell = function(value, row) {
    if (is.na(value)) return(NULL)
    color <- if (value >= 90) "#1a7f37" else if (value >= 70) "#9a6700" else "#cf222e"
    htmltools::tags$span(
      style = paste0("color:", color, ";font-weight:600"),
      sprintf("\%.1f", value)
    )
  }
)
}
