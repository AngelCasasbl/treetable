% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hierarchical-table.R
\name{hierarchical_table}
\alias{hierarchical_table}
\title{Aggregate a flat data frame into a hierarchical table with subtotals}
\usage{
hierarchical_table(data, levels, values, order_col = NULL, level_names = NULL)
}
\arguments{
\item{data}{A flat data frame/tibble.}

\item{levels}{Character vector of column names, from most general to most
specific (e.g. \code{c("group", "subgroup", "category")}).}

\item{values}{Character vector of numeric column names to sum at every
node of the hierarchy.}

\item{order_col}{Optional column name used to order nodes within each
level (defaults to alphabetical order of the level's value).}

\item{level_names}{Optional labels for the \code{level} column at each depth
(defaults to \code{"level_1"}, \code{"level_2"}, ...; the deepest level is always
labeled \code{"leaf"}).}
}
\value{
A long-format tibble, one row per node of the tree, with columns:
\itemize{
\item \code{level}: \code{"level_1"}, \code{"level_2"}, ... (or \code{"leaf"} for the deepest
level).
\item \code{label}: the value of that category at that level.
\item \code{n}: number of original rows that make up that node.
\item \code{row_order}: final order to draw the table top to bottom.
\item one column per entry in \code{levels}, holding that level's value for the
node (\code{NA} if the node sits at a shallower level), useful to
filter/expand on screen.
\item one column per entry in \code{values}, already summed for that node.
}
}
\description{
Groups \code{data} by an arbitrary number of hierarchy columns (from most
general to most specific) and sums one or more numeric columns at every
level, producing subtotal rows for each intermediate level plus a leaf row
for the finest grouping.
}
\details{
\code{data} must be a "flat" data frame (one row per record) with:
\itemize{
\item One column per hierarchy level, from most general to most specific
(e.g. \code{c("group", "subgroup", "category")}). Columns may be character or
factor; \code{NA} is treated as its own category.
\item One or more \emph{numeric} columns to sum at each level (e.g. \code{"income"},
\code{"expense"}, \code{"net"}, or any metric you want to accumulate).
\item (Optional) an ordering column to control the sequence in which
groups/subgroups/categories appear (alphabetical by default).
}
}
\examples{
data <- tibble::tibble(
  area     = c("Sales", "Sales", "Costs"),
  category = c("Product A", "Product B", "Payroll"),
  amount   = c(1000, 500, -300),
  units    = c(10, 5, NA)
)
hierarchical_table(data, levels = c("area", "category"), values = c("amount", "units"))
}
