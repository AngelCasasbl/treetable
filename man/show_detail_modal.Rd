% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detail.R
\name{show_detail_modal}
\alias{show_detail_modal}
\title{Show the per-leaf detail modal for a \code{\link[=tree_table]{tree_table()}} widget}
\usage{
show_detail_modal(id, raw_data, detail, title = NULL, ...)
}
\arguments{
\item{id}{Leaf id, as sent by the widget's \verb{<output_id>_detail} Shiny
input (i.e. \code{input$my_table_detail$id}).}

\item{raw_data}{Raw data forwarded to a custom \code{detail} function; ignored
when \code{detail} is a \code{\link[=detail_table]{detail_table()}} object, which already carries its
own data.}

\item{detail}{Either a \code{\link[=detail_table]{detail_table()}} object (automatic table, filtered
by \code{id}) or a \verb{function(id, raw_data)} returning an
\code{htmltools::tagList()}/Shiny UI to show in the modal.}

\item{title}{Modal title (defaults to \code{id}).}

\item{...}{Additional arguments passed to \code{shiny::modalDialog()}.}
}
\value{
The value of \code{shiny::showModal()}, invisibly.
}
\description{
Wraps the \code{showModal()} + \code{modalDialog()} boilerplate needed to react to a
\code{tree_table()} detail-button click, so a Shiny app doesn't have to rewrite
it for every table. Wire it up with:
}
\details{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{observeEvent(input$my_table_detail, \{
  show_detail_modal(input$my_table_detail$id, raw_data, detail)
\})
}\if{html}{\out{</div>}}
}
