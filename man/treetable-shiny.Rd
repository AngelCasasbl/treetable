% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny.R
\name{treetable-shiny}
\alias{treetable-shiny}
\alias{treetableOutput}
\alias{renderTreetable}
\title{Shiny bindings for \code{\link[=tree_table]{tree_table()}}}
\usage{
treetableOutput(output_id, width = "100\%", height = "auto")

renderTreetable(expr, env = parent.frame(), quoted = FALSE)
}
\arguments{
\item{output_id}{Output variable to read the widget from.}

\item{width, height}{Widget width/height, as a CSS unit string (e.g.
\code{"100\%"}) or \code{"auto"} (default) to size the table to its content with no
internal scroll. Passing a fixed height like \code{"620px"} limits the table
to that height with an internal vertical scrollbar.}

\item{expr}{An expression that returns a \code{\link[=tree_table]{tree_table()}} widget.}

\item{env}{The environment in which to evaluate \code{expr}.}

\item{quoted}{Is \code{expr} a quoted expression (with \code{quote()})? This is
useful if you want to save an expression in a variable.}
}
\value{
\code{treetableOutput()} returns an HTML tag object; \code{renderTreetable()}
returns a \code{shiny.render.function}.
}
\description{
\code{treetableOutput()}/\code{renderTreetable()} are the standard \code{htmlwidgets}
Shiny bindings for \code{\link[=tree_table]{tree_table()}}, generated via
\code{htmlwidgets::shinyWidgetOutput()}/\code{htmlwidgets::shinyRenderWidget()}, so
the widget gets sizing, re-rendering and error handling for free like any
other \code{htmlwidgets}-based output.
}
\details{
A leaf's detail button (see the \code{detail} argument of \code{\link[=tree_table]{tree_table()}}) sets
the Shiny input \code{input[[paste0(outputId, "_detail")]]} to
\verb{list(id = <leaf id>, nonce = <random number>)}; pair it with
\code{\link[=show_detail_modal]{show_detail_modal()}} in an \code{observeEvent()}.
}
\examples{
if (interactive() && rlang::is_installed("shiny")) {
  library(shiny)

  data <- tibble::tibble(
    area = c("Sales", "Sales", "Costs"),
    category = c("Product A", "Product B", "Payroll"),
    id = c("v-a", "v-b", "c-payroll"),
    Q1_budget = c(1000, 500, 300), Q1_actual = c(950, 600, 280)
  )

  ui <- fluidPage(treetableOutput("tbl"))
  server <- function(input, output) {
    output$tbl <- renderTreetable(
      tree_table(
        data,
        levels = c("area", "category"),
        columns = list(
          col_spec(
            "Q1",
            col_field("Q1_budget", label = "Budget"),
            col_field("Q1_actual", label = "Actual")
          )
        )
      )
    )
  }
  shinyApp(ui, server)
}
}
